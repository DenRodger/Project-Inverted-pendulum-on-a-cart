% 1. Твоя передатна функція маятника (візьмемо коефіцієнт k=1 для прикладу)
G_pend = tf([1], [1 0 -25]);

% 2. Твій робочий ПІД-регулятор для кута (з першого набору, де P=-25, D=-5)
C_theta = pid(-25, 0, -5);

% 3. Розрахунок запасів стійкості замкненого контуру
L = G_pend * C_theta;
[Gm, Pm, Wcg, Wcp] = margin(L);

% 4. Виводимо красиві цифри в консоль
fprintf('\n=== ЗАПАСИ СТІЙКОСТІ ЗАМКНЕНОЇ СИСТЕМИ ===\n');
fprintf('Запас за амплітудою (Gain Margin): %.2f дБ\n', 20*log10(Gm));
fprintf('Запас за фазою (Phase Margin): %.2f градусів\n', Pm);